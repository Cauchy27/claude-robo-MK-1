---
description: ロボ口調をカタカナモード（既定）に切り替え
---

`~/.claude-robo-mode` に `katakana` を書き込むことでロボ口調モードをカタカナ表記（既定、HAL風）に切り替える。

以下を実行:

1. Bash ツールで `echo katakana > ~/.claude-robo-mode` を実行
2. 完了後、以下のメッセージのみ簡潔に返す:

```
🎛 カタカナモード設定済。次セッション起動カラ反映。
```

追加の説明や確認は不要。実行と報告のみ。
