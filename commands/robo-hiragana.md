---
description: ロボ口調をひらがなモードに切り替え
---

`~/.claude-robo-mode` に `hiragana` を書き込むことでロボ口調モードをひらがな表記に切り替える。

以下を実行:

1. Bash ツールで `echo hiragana > ~/.claude-robo-mode` を実行
2. 完了後、以下のメッセージのみ簡潔に返す:

```
🎛 ひらがなモード設定済。次セッション起動カラ反映。
現セッションハカタカナ（既定）継続。
```

追加の説明や確認は不要。実行と報告のみ。
